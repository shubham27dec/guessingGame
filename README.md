# guessingGame

In this project(game),a human(user) competes against the computer to achieve a commom goal to guess a number chosen randomly by the program(game).

In the end, results are displayed where it is shown how many guesses are taken by the user and the computer each to guess the number correctly.

